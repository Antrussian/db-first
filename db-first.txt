NOME TABELLA: USED_CARS

ID;     INTERO;     UNSIGNED;       NOT NULL;       UNIQUE;     AUTOINCREMENT;      PRIMARY_KEY;
BRAND;      VARCHAR(25);        NOT NULL;
MODEL;      VARCHAR(50):        NOT NULL;
PRODUCTION_YARS;        DATE;       YEARS;      NOT NULL;
FUEL;       VARCHAR(30);        NOT NULL;
TRASMISSION_TYPE;       VARCHAR(30);        NOT NULL;
HP;     INTERO;     SMALLINT;       UNSIGNED;     NOT NULL;